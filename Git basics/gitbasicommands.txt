git init  - to initialize git in the folder 
git add - to add contents to the staging env 
git commit -m - to commit the files/folders
git status - to check if there are still any changes to be committed 
git log 
git branch local_cat - create local branch
git checkout local_cat - switches to local 
git branch - displays which branch we are in
git checkout master - switches to master
git merge local - merges the changes in local_cat to master in the local
git branch -d <branch name> 

--------------------------------------------------------------------
Git hub 
git hub - create new remote repo 
git remote add origin remote repo url - adding the remote repo url aliasing origin 
(from local_master branch) git push origin master ---- creates a master branch in github
(from local_local_cat) git push origin local_cat  ---- pushes the local_cat branch to github, creates local_cat branch
git pull origin master - to pull the latest changes from the remote master to local repo 
git clone remote repo url - to clone an existing remote repo into my laptop 

---------------------------------------------------------------------

Github 
changes pushed to remote repo 
Create pull request to Master in remote repo 
Merge the pull request

---------------------------------------------------------------------
+ Push local_ local repo -> local_ remo repo 
+ Add content from vig laptop and pushing to remote repo !!!

+ cleaning

+ Pushing from local branch(local_cat) to remote master , have the origin created at local branch and then make the push with below command 
  git push origin local_cat:master